<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>The Deadline of <%= @assignment.name %> Has Been Reached</h1>
    <h2>Students who submitted the assignment:</h2>

    <p>
      The deadline of assignment <%=@assignment.name%> in <%=Group.find_by(id:@assignment.group_id).name%> has been reached.<br>
    </p>
    <p>
      Assignment is mentored by <%=User.find_by(id:Group.find_by(id:@assignment.group_id).mentor_id).name %>
    </p>
    <p>
      Deadline: <%= @assignment.deadline%>
    </p>
  </body>
</html>
