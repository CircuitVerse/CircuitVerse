$("#comment_<%= @comment.id.to_s %>_body_div").html("<%= j(
  render partial: 'body', locals: { comment: @comment }) %>");

$("#comment_<%= @comment.id.to_s %>_updated_timestamp_span").html("<%= j(
  @comment.updated_timestamp) %>");

<%= javascript_proc %>
