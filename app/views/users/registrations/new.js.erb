$("form#lregisterForm").bind("ajax:success", (event) => {
  event.options = [data, status, xhr];
}.on("ajax:error", (event) => {
  console.log(error);
});
//     if data.success
//       //javascript that executes if everything goes o.k.
//       $('#sign_in').modal('hide')
//       $('#sign_in_button').hide()
//       $('#submit_comment').slideToggle(1000, "easeOutBack" )
//     else
//       alert('failure!')
// $()
// console.log(1);
// $("#usersSignupError").html(" #usersSignupError > *");
// console.log(2);
// $("#usersSignupError").style("display", "block");
// console.log(3);
