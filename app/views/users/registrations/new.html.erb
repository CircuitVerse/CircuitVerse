<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6 center-image">
      <%= image_tag "SVGs/signup.svg", alt: "Signup Sketch", :class => "users-image" %>
      <h5>Get yourself registered to start away with saving circuits, building groups and much more!</h5>
    </div>

    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
      <div class="form-container">
        <h1 class="users-heading-main">Sign Up</h1>
        <%= form_with(model: resource, as: resource_name, url: registration_path(resource_name), local: true) do |f| %>
        <%= devise_error_messages! %>
        <div class="field form-group">
          <h6><%= f.label :name %></h6>
          <div class="input-group">
            <%= f.text_field :name, autofocus: true ,class:"form-control form-input" %>
          </div>
        </div>
        <div class="field form-group">
          <h6><%= f.label :email %></h6>
          <div class="input-group">
            <%= f.email_field :email, class:"form-control form-input" %>
          </div>
        </div>
        <div class="field form-group">
          <h6>
            <%= f.label :password %>
            <% if @minimum_password_length %>
              <span>(Minimum <%= @minimum_password_length %> characters)</span>
            <% end %>
          </h6>
        <div class="input-group form-input">
          <%= f.password_field :password, autocomplete: "off" ,class:"form-control users-signup-password-input" %>
          <div class="input-group-addon ml-3 mt-2" onclick="toggleSignupPassword()">
            <i class="far fa-eye-slash password-icon" aria-hidden="true" id="signup-toggle-icon"></i>
          </div>
        </div>
      </div>
        <div class="field form-group">
          <div class="field form-group">
            <div class="input-group">
              <%= f.submit "Sign up" ,class:"btn primary-button users-login-primary-button" %>
              <span class="users-text mt-2 ml-2">Already Registered?</span>
              <%= link_to "Log In", new_user_session_path ,class:"anchor-text mt-2" %>
            </div>
          </div>
          <%= render "users/shared/links" %>
        </div>
        <% end %>
      </div>
    </div>
  </div>
</div>

<script src="/js/togglePassword.js"></script>
