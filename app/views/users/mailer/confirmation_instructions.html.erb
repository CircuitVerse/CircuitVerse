<p><%= t("users.mailer.user_welcome_msg", email: @email) %></p>

<p><%= t("users.mailer.confirm_account_description") %></p>

<p><%= link_to t("users.mailer.link_to_confirm_account"), confirmation_url(@resource, confirmation_token: @token) %></p>
